[build]
  command = "pnpm install --frozen-lockfile && pnpm build"
  publish = "dist/public"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback (in addition to _redirects file)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
